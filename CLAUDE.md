## Agent skills

### Issue tracker

Issues live as GitHub issues at `sripwoud/colporteur`. Use the `gh` CLI for all operations. See `meta/agents/issue-tracker.md`.

### Triage labels

Single-repo vocabulary; canonical names map 1:1 to GitHub labels. See `meta/agents/triage-labels.md`.

### Domain docs

Single-context layout with custom paths: `meta/CONTEXT.md` and `meta/adr/` (both lazily created). See `meta/agents/domain.md`.
